{
    "icon": "icon-people-woman-pregnant",
    "title": {
        "en": "Pregnancy follow up",
        "ny": "Kulondola amayi apakati"
    },
    "context": {
        "person": false,
        "place": false,
        "expression": "contact.contact_type === 'person' && (!contact.sex || contact.sex === 'female') && (!contact.date_of_birth || (ageInYears(contact) >= 12 && ageInYears(contact) < 49)) && user._id != contact._id && summary.pregnant === 'yes' && !contact.date_of_death"
    },
    "hidden_fields": [
        
    ]
}