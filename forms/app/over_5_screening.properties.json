{
    "icon": "icon-people-man",
    "title": {
        "en": "Over 5 Monthly screening",
        "ny": "Kufufuza oposera zaka 5 wa pa mwezi"
    },
    "context": {
        "person": true,
        "place": false,
        "expression": "contact.contact_type === 'person' && user._id != contact._id && (!contact.date_of_birth || ageInYears(contact) >= 5) && !contact.date_of_death && !contact.muted && user.contact_type === 'chw'"
    },
    "hidden_fields": [

    ]
}
