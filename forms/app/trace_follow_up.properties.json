{
    "icon": "icon-followup-general",
    "title": {
        "en": "TRACE follow up",
        "ny": "Kalondolondo wa TRACE"
    },
    "context": {
        "person": true,
        "place": false,
        "expression": "contact.contact_type === 'person' && user._id != contact._id && summary.has_trace_report === 'yes' && !contact.date_of_death && !contact.muted && user.contact_type === 'chw'"
    },
    "hidden_fields": [

    ]
}
