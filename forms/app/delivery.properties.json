{
    "icon": "icon-delivery",
    "title": {
        "en": "Delivery report",
        "ny": "Lipoti la kuchira"
    },
    "context": {
        "person": true,
        "place": false,
        "expression": "contact.contact_type === 'person' && (!contact.sex || contact.sex === 'female') && (!contact.date_of_birth || (ageInYears(contact) >= 12 && ageInYears(contact) < 49)) && user._id != contact._id && summary.pregnant === 'yes' && !contact.date_of_death && !contact.muted && user.contact_type === 'chw'"
    },
    "hidden_fields": [

    ]
}
